# =============================================================================
# GENERAL · GUI SERVER
# =============================================================================
# @group GUI
# @visibility basic
#
# GUI server port (1-65535). CLI flags override this configuration.
CFG_LH_GUI_PORT="3000"

# GUI server host binding. Use "localhost" for local-only, or "0.0.0.0" to expose network access.
CFG_LH_GUI_HOST="localhost"

# Default firewall scope used by the GUI launcher when opening ports.
# Values: "all", "local", or a specific IP/CIDR (e.g. "192.168.1.0/24").
CFG_LH_GUI_FIREWALL_RESTRICTION="local"
