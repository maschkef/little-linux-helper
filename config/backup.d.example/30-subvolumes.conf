# =============================================================================
# BACKUP · SUBVOLUMES
# =============================================================================
# @group Snapshots
# @visibility basic
#
# Space-separated list of BTRFS subvolumes to include in backups.
CFG_LH_BACKUP_SUBVOLUMES="@ @home"

# Automatically merge detected @-prefixed subvolumes from /etc/fstab and /proc/mounts.
CFG_LH_AUTO_DETECT_SUBVOLUMES="true"
