# =============================================================================
# DOCKER · PROJECT DISCOVERY
# =============================================================================
# @group Discovery
# @visibility basic
#
# Root directory scanned for docker-compose projects when check mode = all.
CFG_LH_DOCKER_COMPOSE_ROOT="/opt/containers"

# Directory names to skip during recursive discovery (comma-separated).
CFG_LH_DOCKER_EXCLUDE_DIRS="docker,.docker_archive,node_modules,.git,.cache,venv,__pycache__"

# Maximum directory depth for discovery when scanning all projects.
CFG_LH_DOCKER_SEARCH_DEPTH="3"
