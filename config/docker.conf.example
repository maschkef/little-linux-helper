# config/docker.conf (legacy aggregate)
#
# Docker scan settings are organised in config/docker.d/*.conf fragments. Keep
# this file for legacy compatibility or to provide last-minute overrides. Add
# override assignments below if required.
#
# Example override (force discovery root):
# CFG_LH_DOCKER_COMPOSE_ROOT="/opt/containers"
